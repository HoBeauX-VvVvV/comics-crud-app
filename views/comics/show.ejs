<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/styles.css"/>
  <title><%= comic.title %></title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1><%= comic.title %>, Vol #:<%= comic.issue %></h1> 
    <h2>Written by: <%= comic.author %></h2>
    <h2>Art by: <%= comic.artist %></h2>
  <% if (comic.publisher) { %>
    <p>Publisher: <%= comic.publisher %></p>
  <% } %>
  <% if (comic.year) { %>
    <p>Year: <%= comic.year %></p>
  <% } %>
  <form
    action="/users/<%= user._id %>/comics/<%= comic._id %>/edit">
    <button type="submit">Edit this comic</button>
  </form>
  <form
    action="/users/<%= user._id %>/comics/<%= comic._id %>?_method=DELETE"
    method="POST">
    <button type="submit">Delete this entry</button>
  </form>
  <a class="index" href="/users/<%= user._id %>/comics/">Back to comics index</a>
</body>
</html>
