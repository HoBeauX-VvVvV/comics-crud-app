<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= user.username %>'s comics</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
  <h1>Your comics <%= user.username %></h1>
  <ul>
    <% comics.forEach((comic)=>{ %>
      <li>
        <a href="/users/<%= user._id %>/comics/<%= comic._id %>">
          <%= comic.title %> </a>Vol <%= comic.issue %> by <%= comic.author %>
      </li>
    <% }) %>
  </ul>
  <a href="/users/<%=user._id%>/comics/new">Add a new comic</a>
</body>
</html>